# Valarco Tourism Platform

Monorepo for:
- `frontend/` — customer luxury website
- `admin/` — admin dashboard CMS
- `backend/` — Node.js backend APIs
- `shared/` — shared configs and types
- `docs/` — documentation
- `scripts/` — automation scripts
- `docker/` — Docker configuration
